---
galaxy_info:
  author: Avers90
  description: Manage system users, groups, and SSH authorized keys
  license: MIT
  min_ansible_version: "2.10"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm

dependencies: []

allow_duplicates: false

# Required collections:
#   ansible-galaxy collection install ansible.posix
